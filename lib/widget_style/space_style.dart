import 'package:flutter/material.dart';

class SpaceStyle {
  static const EdgeInsets none = EdgeInsets.all(0);
  static const allTiny = EdgeInsets.all(2.5);
  static const allSmall = EdgeInsets.all(5.0);
  static const allBasic = EdgeInsets.all(10.0);
  static const allBig = EdgeInsets.all(20.0);
  static const verticalTiny = EdgeInsets.symmetric(vertical: 2.5);
  static const verticalSmall = EdgeInsets.symmetric(vertical: 5.0);
  static const verticalBasic = EdgeInsets.symmetric(vertical: 10.0);
  static const verticalBig = EdgeInsets.symmetric(vertical: 20.0);
  static const horizontalBasic = EdgeInsets.symmetric(horizontal: 10.0);
  static const horizontalBig = EdgeInsets.symmetric(horizontal: 20.0);
  static const horizontalTiny = EdgeInsets.symmetric(horizontal: 2.5);
  static const topTiny = EdgeInsets.only(top: 2.5);
  static const topSmall = EdgeInsets.only(top: 5);
  static const topBasic = EdgeInsets.only(top: 10);
  static const topBig = EdgeInsets.only(top: 20);
  static const botBasic = EdgeInsets.only(bottom: 10);
  static const botBig = EdgeInsets.only(bottom: 20);
  static const botSmall = EdgeInsets.only(bottom: 5);
  static const rightTiny = EdgeInsets.only(right: 2.5);
  static const rightSmall = EdgeInsets.only(right: 5.0);
  static const rightBasic = EdgeInsets.only(right: 10);
  static const rightBig = EdgeInsets.only(right: 20);
  static const leftTiny = EdgeInsets.only(left: 2.5);
  static const leftBasic = EdgeInsets.only(left: 10);

  //for specific widget
  static const status = EdgeInsets.only(top: 0.5, bottom: 0.5, left: 5, right: 20);
}
