import 'package:flutter/material.dart';

class ShapeStyle {
  static const RoundedRectangleBorder none = RoundedRectangleBorder(borderRadius: BorderRadius.zero),
      circularSmall = RoundedRectangleBorder(borderRadius: BorderRadius.all(Radius.circular(5)));
}
